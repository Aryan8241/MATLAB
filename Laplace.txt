clc;
clear all;
close all;

% Define symbolic variables
syms t s

% Define the signal x(t)
x = exp(-2*t);   % e^(-2t)

% Compute Laplace Transform
X = laplace(x, t, s);

% Display result
disp('Laplace Transform of the signal X(s) = ');
disp(X);
